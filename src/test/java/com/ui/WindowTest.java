package com.ui;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class WindowTest {
  /*
   * @Test
   * 
   * @DisplayName("JMenuBar must not be null")
   * public void jmenuNotNull() {
   * Window window = new Window();
   * Assertions.assertNotNull(window.addMenuBar());
   * }
   * 
   * @Test
   * 
   * @DisplayName("fileMenu must not be null")
   * public void fileMenuNotNull() {
   * Window window = new Window();
   * Assertions.assertNotNull(window.fileMenu());
   * }
   * 
   * @Test
   * 
   * @DisplayName("editMenu must not be null")
   * public void editMenuNotNull() {
   * Window window = new Window();
   * Assertions.assertNotNull(window.editMenu());
   * }
   * 
   * @Test
   * 
   * @DisplayName("settingMenu must not be null")
   * public void settingMenuNotNull() {
   * Window window = new Window();
   * Assertions.assertNotNull(window.settingMenu());
   * }
   */
}
